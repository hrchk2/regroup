<div class="container">
  <div class="row mt-3">
    <div class="col-10 offset-1 col-sm-6 offset-sm-3 mt-3 mb-2 ">
        <h1>管理者 検索一覧</h1>
    </div>
  </div>

  <div class="row mt-2">
    <% if @range == "Tag" %>
      <%= render "admin/posts/index", posts: @tag_posts %>
   <% else %>
      <%= render "admin/posts/index", posts: @posts %>
    <% end %>
  </div>
</div>